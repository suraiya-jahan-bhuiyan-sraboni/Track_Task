<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme.
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!- Set the splash screen background, animated icon, and animation
        duration.
        <item name="windowSplashScreenBackground">@color/white</item>

        <!- Use windowSplashScreenAnimatedIcon to add a drawable or an animated
             drawable. One of these is required.
        <item name="windowSplashScreenAnimatedIcon">@drawable/baseline_how_to_reg_24</item>
        <!- Required for animated icons.
        <item name="windowSplashScreenAnimationDuration">200</item>

        <!- Set the theme of the Activity that directly follows your splash
        screen. This is required
        <item name="postSplashScreenTheme">@style/Theme.TrackTask</item>
    </style>-->
    <style name="Base.Theme.TrackTask" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/green</item>
    </style>

    <style name="Theme.TrackTask" parent="Base.Theme.TrackTask" />
</resources>